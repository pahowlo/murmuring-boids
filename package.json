{
  "name": "murmuring-boids",
  "version": "0.8.4",
  "author": "pahowlo <paul.legoux@gmail.com>",
  "description": "A JS component for rendering an interactive murmuring boids animation.",
  "type": "module",
  "main": "./src/main.ts",
  "exports": "./src/main.ts",
  "dependencies": {
    "gl-matrix": "3.4.3"
  },
  "scripts": {
    "build": "./.scripts/build",
    "clean": "rm -rf node_modules/ dist/ target/",
    "demo": "vite",
    "format": "prettier --config package.json --write 'src/**/*.{js,ts,json}'",
    "check": "tsc --noEmit",
    "test:init": "bun playwright install firefox",
    "test": "vitest run --browser --browser.headless"
  },
  "prettier": {
    "printWidth": 100,
    "semi": false,
    "singleQuote": false
  },
  "devDependencies": {
    "@rollup/rollup-darwin-arm64": "4.53.1",
    "@vitest/browser-playwright": "4.0.8",
    "playwright": "1.56.1",
    "prettier": "3.6.2",
    "typescript": "5.9.2",
    "vite": "7.0.6",
    "vitest": "4.0.8"
  }
}
